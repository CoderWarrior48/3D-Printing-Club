import Carousel from "../components/Carousel/Carousel";

const AdminDashboard = () => {
  return (
    <p>If you're an admin, you alone have access to this page. The plan is to have this as a dashboard with all nessecary controls in one place.</p>
  );
};

export default AdminDashboard;
